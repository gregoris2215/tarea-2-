@page "/ej03"
@rendermode InteractiveServer

<h5>Construir una función que reciba como parámetro un entero y retorno, el carácter al cual pertenece ese entero como código ASCII.</h5>

<input @bind="numA" type="number" placeholder="Ingresa un número" />
<button @onclick="ConvertToAscii">Convertir</button>

<p>El carácter ASCII es: @asciiCharacter</p>

@code {
    private int numA;
    private char asciiCharacter;

    private void ConvertToAscii()
    {
        asciiCharacter = GetAsciiCharacter(numA);
    }

    private char GetAsciiCharacter(int numA)
    {
        return Convert.ToChar(numA);
    }
}