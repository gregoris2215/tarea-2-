@page "/ej017"
@rendermode InteractiveServer

<h5>Construir una función que reciba como parámetro un vector de 10 posiciones enteras y retorne a la posición del número entero que tenga mayor cantidad de dígitos.</h5>

@for (int i = 0; i < 10; i++)
{
    <input @bind="numArray[i]" type="number" placeholder="Ingresa un número" />
}

<button @onclick="FindMaxDigitPosition">Encontrar posición</button>

<p>La posición del número con más dígitos es: @maxDigitPosition</p>

@code {
    private int[] numArray = new int[10];
    private int maxDigitPosition = -1;

    private void FindMaxDigitPosition()
    {
        maxDigitPosition = GetMaxDigitPosition(numArray);
    }

    private int GetMaxDigitPosition(int[] numA)
    {
        int numB = -1;
        int numC = 0;

        for (int numD = 0; numD < numA.Length; numD++)
        {
            int numEDigitCount = numA[numD].ToString().Length;
            if (numEDigitCount > numC)
            {
                numC = numEDigitCount;
                numB = numD;
            }
        }

        return numB;
    }
}